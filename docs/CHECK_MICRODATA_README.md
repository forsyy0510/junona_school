# Скрипт проверки микроразметки

Скрипт `check_microdata.py` проверяет наличие и правильность использования атрибутов `itemprop` на всех страницах информационных разделов сайта.

## Установка зависимостей

Перед использованием скрипта убедитесь, что установлены все зависимости:

```bash
pip install -r requirements.txt
```

Особенно важна библиотека `beautifulsoup4` для парсинга HTML.

## Использование

### Проверка всех разделов

```bash
python check_microdata.py
```

### Проверка конкретного раздела

```bash
python check_microdata.py --endpoint main
python check_microdata.py --endpoint education
python check_microdata.py --endpoint documents
```

### Сохранение отчета в файл

```bash
python check_microdata.py --output report.txt
python check_microdata.py --endpoint main --output main_report.txt
```

## Что проверяет скрипт

1. **Наличие обязательных атрибутов** - проверяет, что все обязательные `itemprop` атрибуты присутствуют на странице
2. **Структура вложенности** - проверяет, что дочерние теги находятся внутри главных тегов
3. **Правильность использования itemscope** - проверяет, что главные теги имеют атрибут `itemscope` и `itemtype`
4. **Табличная структура** - проверяет, что данные отображаются в табличной структуре (как в документации)

## Ожидаемые атрибуты

Скрипт проверяет следующие разделы:

- **Основные сведения** (`main`) - `mainInfo`, `fullName`, `shortName`, `regDate`, `address`, `telephone`, `email`, `workTime`, `uchredLaw`
- **Структура** (`structure`) - `structOrgUprav`, `filInfo`, `repInfo` с соответствующими дочерними тегами
- **Документы** (`documents`) - `documents`, `ustavDocLink`, `licenseDocLink`, `accreditationDocLink` и др.
- **Образование** (`education`) - `eduAccred`, `eduOp`, `educationPlan`, `educationSchedule` и др.
- **Стандарты** (`standards`) - `eduStandards`, `eduFedDoc`, `eduStandartDoc` и др.
- **Руководство** (`management`) - `management`, `rucovodstvo`, `rucovodstvoZam`
- **Педагогический состав** (`teachers`) - `teachingStaff`, `teacher`, `qualification`
- **Материально-техническое обеспечение** (`facilities`) - `facilities`, `purposeCab`, `purposeLibr`, `purposeSport` и др.
- **Стипендии** (`scholarships`) - `scholarships`, `grant`, `support`, `localAct`
- **Платные услуги** (`paid-services`) - `paidEduServices`, `paidEdu`, `paidDog`, `paidSt`
- **Финансы** (`finance`) - `financialActivity`, `volume`, `finPost`, `finRas`, `finPlanDocLink`
- **Вакансии** (`vacancies`) - `vacantPlaces`, `vacant`
- **Международное сотрудничество** (`international`) - `internationalCooperation`, `internationalDog`, `partner`
- **Питание** (`food`) - `cateringOrganization`, `meals`

## Пример вывода

```
================================================================================
ПРОВЕРКА МИКРОРАЗМЕТКИ (ITEMPROP АТРИБУТОВ)
================================================================================

Проверка раздела: Основные сведения (main)
URL: /sveden/main
✅ Проблем не найдено
📊 Статистика:
   - Найдено атрибутов itemprop: 12
   - Найдено itemscope: 2
   - Найдено таблиц: 1
   - Найденные атрибуты: mainInfo, fullName, shortName, regDate, address, telephone, email, workTime, uchredLaw

================================================================================
ИТОГОВЫЙ ОТЧЕТ
================================================================================
Всего разделов проверено: 14
Разделов с проблемами: 2
Разделов без проблем: 12
Всего найдено проблем: 5
```

## Устранение проблем

Если скрипт находит проблемы:

1. **Отсутствующие обязательные атрибуты** - убедитесь, что все обязательные поля заполнены в разделе
2. **Неправильная структура вложенности** - проверьте, что дочерние теги находятся внутри главных тегов
3. **Отсутствие itemscope** - главные теги должны иметь атрибут `itemscope` и `itemtype`
4. **Старая структура** - убедитесь, что используется табличная структура, а не `form-data-grid`

## Примечания

- Скрипт использует Flask test client для рендеринга страниц, поэтому требует активного контекста приложения
- Скрипт проверяет только HTML структуру, не проверяет содержимое полей
- Для полной проверки рекомендуется использовать официальный инструмент VIKON

