# 🔄 Руководство по обновлению проекта на Render.com

## Способ 1: Автоматический деплой из GitHub (Рекомендуется) ⭐

Если ваш проект подключен к GitHub репозиторию, Render автоматически обновляет сайт при каждом push.

### Шаги:

1. **Внесите изменения в код локально**

2. **Закоммитьте и запушьте изменения:**
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push origin main
   ```

3. **Render автоматически обнаружит изменения:**
   - Перейдите в [Render Dashboard](https://dashboard.render.com)
   - Выберите ваш Web Service
   - В разделе "Events" вы увидите новый деплой
   - Дождитесь завершения (обычно 2-5 минут)

4. **Проверьте результат:**
   - После завершения деплоя сайт автоматически обновится
   - URL останется прежним: `https://site-junona.onrender.com`

---

## Способ 2: Ручной деплой через Dashboard

Если автоматический деплой не настроен или нужно пересобрать вручную:

1. **Войдите в [Render Dashboard](https://dashboard.render.com)**

2. **Выберите ваш Web Service** (например, `site-junona`)

3. **Нажмите кнопку "Manual Deploy"** в правом верхнем углу

4. **Выберите:**
   - **"Deploy latest commit"** - для деплоя последнего коммита
   - **"Clear build cache & deploy"** - если есть проблемы с кэшем

5. **Дождитесь завершения деплоя**

---

## Способ 3: Обновление через Render CLI

### Установка Render CLI:

```bash
# Windows (PowerShell)
iwr https://render.com/install.ps1 -useb | iex

# macOS/Linux
curl -fsSL https://render.com/install.sh | sh
```

### Использование:

```bash
# Войдите в систему
render login

# Перейдите в директорию проекта
cd d:/project/site_junona

# Обновите сервис
render deploy
```

---

## 🔧 Обновление базы данных

Если вы изменили структуру базы данных (добавили новые модели, поля):

### Вариант А: Через Render Shell

1. В Render Dashboard выберите ваш Web Service
2. Перейдите в раздел "Shell"
3. Выполните команды:

```bash
# Создание новых таблиц
python -c "from app import app, db; app.app_context().push(); db.create_all()"

# Или если нужно создать раздел "Образование"
python check_education_section.py
```

### Вариант Б: Через локальную миграцию

1. Обновите структуру БД локально
2. Экспортируйте данные (если нужно)
3. Импортируйте на Render через Shell или через админ-панель сайта

---

## 📝 Обновление переменных окружения

Если нужно изменить переменные окружения (SECRET_KEY, DATABASE_URL и т.д.):

1. В Render Dashboard выберите ваш Web Service
2. Перейдите в раздел **"Environment"**
3. Нажмите **"Add Environment Variable"**
4. Добавьте или измените переменные:
   - `SECRET_KEY` - секретный ключ Flask
   - `FLASK_DEBUG` - режим отладки (False для production)
   - `DATABASE_URL` - обычно устанавливается автоматически
5. Нажмите **"Save Changes"**
6. Render автоматически перезапустит сервис

---

## 🗄️ Обновление данных в базе данных

### Через админ-панель сайта:

1. Войдите на сайт как администратор
2. Используйте мастер заполнения или формы редактирования
3. Изменения сохраняются в базе данных автоматически

### Через Render Shell:

1. Откройте Shell в Render Dashboard
2. Запустите Python:

```bash
python
```

3. Выполните команды:

```python
from app import app, db
from info.models import InfoSection
from models.models import PageContent

with app.app_context():
    # Пример: создание раздела
    section = InfoSection(
        endpoint='education',
        url='/sveden/education',
        title='Образование',
        text='{}'
    )
    db.session.add(section)
    db.session.commit()
    print("Раздел создан!")
```

---

## 🔄 Обновление зависимостей

Если вы добавили новые пакеты в `requirements.txt`:

1. **Обновите `requirements.txt` локально:**
   ```bash
   pip freeze > requirements.txt
   ```

2. **Закоммитьте и запушьте:**
   ```bash
   git add requirements.txt
   git commit -m "Обновлены зависимости"
   git push origin main
   ```

3. **Render автоматически установит новые зависимости** при следующем деплое

---

## 🐛 Решение проблем при обновлении

### Проблема: Деплой не запускается автоматически

**Решение:**
- Проверьте настройки в Render Dashboard → Settings → "Auto-Deploy"
- Убедитесь, что выбрана правильная ветка (обычно `main` или `master`)
- Проверьте, что изменения действительно запушены в GitHub

### Проблема: Ошибки при сборке

**Решение:**
1. Проверьте логи деплоя в Render Dashboard → "Events" → выберите неудачный деплой
2. Частые причины:
   - Ошибки в коде (проверьте синтаксис)
   - Отсутствующие зависимости в `requirements.txt`
   - Проблемы с путями к файлам
3. Исправьте ошибки и запушьте снова

### Проблема: Изменения не применяются

**Решение:**
1. Очистите кэш сборки: "Manual Deploy" → "Clear build cache & deploy"
2. Проверьте, что изменения действительно в репозитории
3. Убедитесь, что деплой завершился успешно (зеленый статус)

### Проблема: База данных не обновляется

**Решение:**
1. Используйте Render Shell для выполнения миграций
2. Проверьте, что `DATABASE_URL` настроен правильно
3. Убедитесь, что у приложения есть доступ к базе данных

---

## 📊 Мониторинг обновлений

### Просмотр истории деплоев:

1. В Render Dashboard выберите ваш Web Service
2. Перейдите в раздел **"Events"**
3. Вы увидите список всех деплоев с:
   - Временем деплоя
   - Статусом (успешно/неудачно)
   - Коммитом и автором
   - Логами сборки

### Просмотр логов:

1. В Render Dashboard выберите ваш Web Service
2. Перейдите в раздел **"Logs"**
3. Вы увидите логи работы приложения в реальном времени

---

## ⚡ Быстрая команда для обновления

Создайте файл `update.sh` (или `update.bat` для Windows):

### Windows (update.bat):
```batch
@echo off
echo Обновление проекта на Render...
git add .
git commit -m "Обновление: %date% %time%"
git push origin main
echo Изменения отправлены. Render автоматически обновит сайт.
pause
```

### Linux/macOS (update.sh):
```bash
#!/bin/bash
echo "Обновление проекта на Render..."
git add .
git commit -m "Обновление: $(date)"
git push origin main
echo "Изменения отправлены. Render автоматически обновит сайт."
```

Использование:
```bash
# Windows
update.bat

# Linux/macOS
chmod +x update.sh
./update.sh
```

---

## ✅ Чеклист перед обновлением

- [ ] Проверено локально, что изменения работают
- [ ] Обновлен `requirements.txt` (если добавлены новые зависимости)
- [ ] Проверен синтаксис кода
- [ ] Закоммичены все изменения
- [ ] Изменения запушены в GitHub
- [ ] Проверен статус деплоя в Render Dashboard
- [ ] Проверена работа сайта после деплоя

---

## 🔗 Полезные ссылки

- [Render Dashboard](https://dashboard.render.com)
- [Render Documentation](https://render.com/docs)
- [Render Status Page](https://status.render.com)

---

## 💡 Советы

1. **Всегда тестируйте локально** перед пушем на Render
2. **Используйте отдельную ветку** для тестирования больших изменений
3. **Проверяйте логи** после каждого деплоя
4. **Создавайте резервные копии** базы данных перед миграциями
5. **Используйте переменные окружения** для конфигурации, а не хардкод

---

**Последнее обновление:** 23.12.2025

