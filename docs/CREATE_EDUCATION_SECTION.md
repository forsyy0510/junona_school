# Создание раздела "Образование"

## Проблема

Раздел "Образование" отсутствует в базе данных, хотя URL `/sveden/education` настроен и должен работать.

## ✅ Решение 1: Автоматическое создание (уже добавлено)

Раздел "Образование" теперь создается автоматически при запуске приложения. Просто перезапустите приложение:

```bash
python app.py
```

Или на Render.com:
- Раздел будет создан автоматически при следующем деплое
- Или перезапустите сервис в Render Dashboard

## ✅ Решение 2: Ручное создание через скрипт

Запустите скрипт проверки и создания:

```bash
python check_education_section.py
```

Скрипт:
- Проверит наличие раздела "Образование"
- Создаст его, если отсутствует
- Покажет статус всех обязательных разделов

## ✅ Решение 3: Создание через мастер заполнения

1. Войдите в систему как администратор
2. Перейдите в раздел "Сведения об образовательной организации"
3. Нажмите кнопку **"⚡ Мастер"**
4. В списке разделов выберите **"Образование"**
5. Заполните все обязательные поля:
   - Реализуемые образовательные программы
   - Учебные планы (НОО, ООО, СОО)
   - Календарный учебный график
   - Численность обучающихся
   - Языки образования
   - И т.д.
6. Нажмите **"Сохранить"**

## ✅ Решение 4: Создание через админ-панель

1. Перейдите в `/admin/sitemap`
2. Найдите раздел "Образование" в списке
3. Если его нет, создайте новый раздел:
   - Endpoint: `education`
   - URL: `/sveden/education`
   - Title: `Образование`
4. Заполните форму и сохраните

## ✅ Решение 5: Создание через Python консоль

Откройте Python консоль в Render Shell или локально:

```python
from app import app, db
from info.models import InfoSection
import json

with app.app_context():
    # Проверяем наличие
    section = InfoSection.query.filter_by(endpoint='education').first()
    
    if not section:
        # Создаем раздел
        section = InfoSection(
            endpoint='education',
            url='/sveden/education',
            title='Образование',
            text=json.dumps({
                'form_data': {
                    'title': 'Образование',
                    'implemented_programs': '',
                    'adapted_programs': '',
                    'curriculum_noo': '',
                    'curriculum_ooo': '',
                    'curriculum_soo': '',
                    'calendar_schedule': '',
                    'student_numbers_noo': '',
                    'student_numbers_ooo': '',
                    'student_numbers_soo': '',
                    'student_numbers_document': '',
                    'education_languages': '',
                    'professional_programs': '',
                    'graduate_employment': ''
                }
            }, ensure_ascii=False),
            content_blocks=json.dumps([], ensure_ascii=False)
        )
        db.session.add(section)
        db.session.commit()
        print("✅ Раздел 'Образование' создан!")
    else:
        print("✅ Раздел 'Образование' уже существует!")
```

## 🔍 Проверка

После создания раздела проверьте:

1. **Доступность по URL:**
   - Откройте: `https://site-junona.onrender.com/sveden/education`
   - Должна открыться страница "Образование"

2. **В меню:**
   - Перейдите в "Сведения об образовательной организации"
   - В выпадающем меню должна быть ссылка "Образование"

3. **В базе данных:**
   ```python
   from app import app, db
   from info.models import InfoSection
   
   with app.app_context():
       section = InfoSection.query.filter_by(endpoint='education').first()
       if section:
           print(f"✅ Раздел найден: {section.title}")
           print(f"   URL: {section.url}")
       else:
           print("❌ Раздел не найден")
   ```

## ⚠️ Важно

- Раздел должен иметь `endpoint='education'` (не 'education-section' или другое)
- URL должен быть `/sveden/education` (соответствует маппингу в `ENDPOINT_MAPPING`)
- После создания раздела заполните его через мастер заполнения

## 📝 После создания

1. Заполните раздел через мастер заполнения
2. Загрузите все необходимые документы
3. Проверьте работу страницы
4. Повторно проверьте сайт программой VIKON

---

**Примечание:** Теперь раздел "Образование" создается автоматически при запуске приложения. Если он все еще отсутствует, используйте один из способов выше.

