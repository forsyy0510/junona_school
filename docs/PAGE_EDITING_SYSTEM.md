# Система редактирования страниц

## Обзор

Реализована система редактирования контента для основных страниц сайта:
- Главная страница (`/`)
- Ближайшие события
- Информация (`/info`)
- Проекты (`/projects`)
- Фотоальбомы (`/albums`)
- Контакты (`/contacts`)
- О школе (`/about`)

## Технические детали

### Модель данных

**Файл:** `models/models.py`

Создана модель `PageContent` для хранения контента страниц:
- `page_key` - уникальный ключ страницы (index, events, info, projects, albums, contacts, about)
- `content` - JSON строка с контентом страницы
- `updated_at` - дата последнего обновления

Методы:
- `get_content()` - получить контент как словарь
- `set_content(data)` - установить контент из словаря
- `get_or_create(page_key, default_content=None)` - получить или создать запись

### Маршруты редактирования

**Файл:** `main/routes.py`

Добавлен маршрут `/edit/<page_key>` для редактирования страниц:
- Требует авторизации (`@login_required`)
- Поддерживает GET (отображение формы) и POST (сохранение данных)
- Обрабатывает JSON и form-data
- Валидирует `page_key` (разрешенные страницы)

### Шаблоны редактирования

Созданы шаблоны в `templates/main/`:
- `edit_index.html` - редактирование главной страницы
- `edit_events.html` - редактирование событий
- `edit_contacts.html` - редактирование контактов
- `edit_about.html` - редактирование страницы "О школе"
- `edit_projects.html` - редактирование проектов
- `edit_albums.html` - редактирование фотоальбомов
- `edit_info.html` - информационная страница (использует мастер заполнения)

### Обновленные шаблоны

Все основные шаблоны обновлены для:
- Отображения контента из базы данных через `page_content`
- Добавления кнопок редактирования для авторизованных пользователей
- Использования значений по умолчанию, если контент отсутствует

## Структура данных

### Главная страница (index)
```json
{
  "welcome_text": "Текст приветствия",
  "achievements": ["Достижение 1", "Достижение 2"],
  "it_infrastructure": ["Элемент 1", "Элемент 2"],
  "teachers": ["Пункт 1", "Пункт 2"],
  "partnership": ["Партнер 1", "Партнер 2"],
  "stats": {
    "students": "500+",
    "teachers": "50+",
    "years": "15+",
    "success_rate": "95%"
  },
  "events": [
    {"date": "Сентябрь 2024", "text": "Описание события"}
  ],
  "achievements_list": ["Достижение 1", "Достижение 2"],
  "partners": ["Партнер 1", "Партнер 2"]
}
```

### Контакты (contacts)
```json
{
  "address": "Адрес",
  "phone": "Телефон",
  "email": "Email",
  "work_hours": "Время работы"
}
```

### О школе (about)
```json
{
  "history": "Текст истории",
  "mission": "Текст миссии",
  "achievements": ["Достижение 1", "Достижение 2"]
}
```

### Проекты (projects)
```json
{
  "projects": [
    {
      "id": 1,
      "title": "Название проекта",
      "description": "Описание",
      "status": "Статус"
    }
  ]
}
```

### Фотоальбомы (albums)
```json
{
  "albums": [
    {
      "id": 1,
      "title": "Название альбома",
      "photo_count": "12 фото"
    }
  ]
}
```

## Использование

### Для администраторов

1. Войдите в систему
2. Перейдите на нужную страницу
3. Нажмите кнопку "✏️ Редактировать"
4. Внесите изменения
5. Нажмите "💾 Сохранить"

### Редактирование главной страницы

- Приветственный текст
- Списки достижений, IT-инфраструктуры, педагогов, партнерства
- Статистика (учащиеся, педагоги, годы работы, процент поступления)
- Ближайшие события (дата и описание)
- Список последних достижений
- Партнеры

### Редактирование событий

События редактируются отдельно через кнопку "📅 Редактировать события" на главной странице. Изменения сохраняются в контент главной страницы.

## Особенности реализации

1. **Значения по умолчанию**: Если контент отсутствует, используются предустановленные значения
2. **Безопасность**: Редактирование доступно только авторизованным пользователям
3. **Валидация**: Проверка типов данных и структуры JSON
4. **Обработка ошибок**: Try-catch блоки с откатом транзакций
5. **AJAX**: Сохранение через fetch API без перезагрузки страницы

## Миграция базы данных

Таблица `page_content` создается автоматически при первом запуске приложения через `db.create_all()`.

Для создания таблицы вручную:
```python
from app import app, db
from models.models import PageContent
with app.app_context():
    db.create_all()
```

## Стиль и дизайн

Все формы редактирования выполнены в едином стиле сайта:
- Градиентные кнопки
- Карточки с закругленными углами
- Адаптивный дизайн
- Интуитивный интерфейс
- Валидация на клиенте и сервере

