# Исправление ошибки JavaScript

## 🐛 **Проблема**
В консоли браузера возникала ошибка:
```
Uncaught ReferenceError: toggleInputType is not defined
```

## 🔍 **Причина**
Функция `toggleInputType` была определена в `wizard.js`, но не экспортировалась в глобальную область видимости, что приводило к ошибке при попытке её вызова из HTML.

## ✅ **Решение**

### 1. **Добавлен экспорт функции в `wizard.js`**
```javascript
// Экспортируем функцию toggleInputType для глобального доступа
window.toggleInputType = (fieldName, type) => {
    if (window.wizardManager && typeof window.wizardManager.toggleInputType === 'function') {
        return window.wizardManager.toggleInputType(fieldName, type);
    }
};
```

### 2. **Убрано дублирование из `app.js`**
Удален дублирующий код экспорта функции, так как теперь она экспортируется из `wizard.js`.

### 3. **Убрано дублирование из `base.html`**
Закомментирована строка экспорта функции, так как теперь она экспортируется из `wizard.js`.

## 🎯 **Результат**

- ✅ **Ошибка `toggleInputType is not defined` исправлена**
- ✅ **Функция корректно экспортируется в глобальную область видимости**
- ✅ **Убрано дублирование кода**
- ✅ **Улучшена архитектура - функция экспортируется из одного места**

## 🔧 **Как это работает**

1. **Инициализация**: При загрузке страницы `wizard.js` создает экземпляр `WizardManager`
2. **Экспорт**: Функция `toggleInputType` экспортируется в `window.toggleInputType`
3. **Вызов**: HTML может вызывать `toggleInputType(fieldName, type)` без ошибок
4. **Делегирование**: Функция делегирует вызов к методу `wizardManager.toggleInputType()`

## 📝 **Файлы изменены**

- `static/wizard.js` - добавлен экспорт функции
- `static/app.js` - убрано дублирование
- `templates/base.html` - убрано дублирование

Теперь система работает без ошибок JavaScript! 🎉
