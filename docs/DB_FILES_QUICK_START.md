# Хранение файлов в БД - Быстрый старт

## ✅ Что сделано

Система хранения файлов в базе данных полностью реализована:

1. ✅ **Модель обновлена** - добавлено поле `file_data` (BLOB) для хранения файлов
2. ✅ **FileManager обновлен** - все файлы сохраняются в БД автоматически
3. ✅ **Роуты скачивания обновлены** - файлы отдаются из БД
4. ✅ **Специальные ссылки для питания** - `/food/<filename>` работает с БД
5. ✅ **API для питания обновлено** - работает с БД

## 🚀 Как это работает

### Загрузка файла

При загрузке файла через веб-интерфейс:
1. Файл автоматически сохраняется в БД (поле `file_data`)
2. Метаданные сохраняются в таблице `info_file`
3. Флаг `stored_in_db=True` устанавливается автоматически

### Скачивание файла

При скачивании файла:
1. Система проверяет, хранится ли файл в БД
2. Если да - отдает из БД через `BytesIO`
3. Если нет - ищет в файловой системе (обратная совместимость)

### Удаление файла

При удалении файла:
1. Файл удаляется из БД (включая `file_data`)
2. Запись удаляется из таблицы `info_file`
3. Если файл был в файловой системе - удаляется и оттуда

## 📋 Специальные ссылки для питания

Файлы питания используют специальный URL `/food/<filename>`:

- Автоматически определяется по `section_endpoint == 'food'` или `field_name == 'menu_file'`
- Поддерживает хранение в БД
- Сохраняет обратную совместимость с файловой системой

## ⚠️ Важно

### Ограничения БД

- **SQLite**: до 2GB на файл (не рекомендуется для файлов >10MB)
- **PostgreSQL**: до 1GB на файл (BYTEA) или неограниченно (Large Objects)

### Рекомендации

- Для файлов <10MB - БД отлично подходит
- Для файлов >10MB - рекомендуется облачное хранилище (S3, Cloudinary)

## 🔧 Миграция базы данных

После деплоя нужно обновить структуру БД:

```python
# В Python shell или через миграцию
from app import app, db
with app.app_context():
    db.create_all()  # Создаст новые поля
```

## ✅ Проверка

1. **Загрузите файл** через веб-интерфейс
2. **Проверьте БД:**
   ```sql
   SELECT filename, file_size, stored_in_db FROM info_file WHERE filename = 'your_file.pdf';
   ```
3. **Проверьте скачивание:**
   - Откройте ссылку на файл
   - Файл должен скачаться

## 🎉 Готово!

Все новые файлы будут автоматически сохраняться в БД. Файлы не будут пропадать на Render.com!

---

**Подробная документация:** `docs/DB_FILE_STORAGE.md`

