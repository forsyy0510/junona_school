# Полный отчет по тегам микроразметки согласно методическим рекомендациям 2024

**Дата создания:** 2024  
**Источник:** Методические рекомендации представления информации об образовательной организации в открытых источниках с учетом соблюдения требований законодательства в сфере образования (2024)

---

## Общая структура

### Главный контейнер (обязателен для всех страниц раздела "Сведения")

```html
<div class="section-container" itemscope itemtype="https://schema.org/EducationalOrganization">
    <h1 itemprop="name">{{ section.title }}</h1>
    <!-- Все подразделы внутри -->
</div>
```

**Требования:**
- ✅ `itemscope` - обязателен
- ✅ `itemtype="https://schema.org/EducationalOrganization"` - обязателен
- ✅ `itemprop="name"` на заголовке `<h1>` - обязателен

---

## РАЗДЕЛ 1: Основные сведения (`/sveden/common` → endpoint: `main`)

### Контейнер раздела

```html
<div class="form-data-section" itemprop="mainInfo" itemscope itemtype="https://schema.org/EducationalOrganization">
```

**Требования:**
- ✅ `itemprop="mainInfo"` - связь с родительским контейнером
- ✅ `itemscope` - обязателен
- ✅ `itemtype="https://schema.org/EducationalOrganization"` - обязателен

### Обязательные поля (все внутри контейнера `mainInfo`)

| № | Проверяемая информация | Применяемые атрибуты | Статус |
|---|------------------------|---------------------|--------|
| 1 | Версия сайта для слабовидящих | `itemprop="copy"` | ⚠️ Не реализовано |
| 2 | Полное наименование | `itemprop="fullName"` | ✅ Реализовано |
| 3 | Сокращенное наименование | `itemprop="shortName"` | ✅ Реализовано |
| 4 | Дата создания | `itemprop="regDate"` | ✅ Реализовано |
| 5 | Адрес местонахождения | `itemprop="address"` | ✅ Реализовано |
| 6 | Режим и график работы | `itemprop="workTime"` | ✅ Реализовано |
| 7 | Контактные телефоны | `itemprop="telephone"` | ✅ Реализовано |
| 8 | Адреса электронной почты | `itemprop="email"` | ✅ Реализовано |
| 9 | Лицензия | `itemprop="licenseDocLink"` + `itemscope itemtype="https://schema.org/MediaObject"` | ✅ Реализовано |
| 10 | Аккредитация | `itemprop="accreditationDocLink"` + `itemscope itemtype="https://schema.org/MediaObject"` | ✅ Реализовано |

### Учредители (главный тег `uchredLaw`)

**Родительский контейнер:**
```html
<div itemprop="uchredLaw">
```

**Дочерние теги (все 5 обязательны):**

| № | Проверяемая информация | Применяемые атрибуты | Статус |
|---|------------------------|---------------------|--------|
| 5.1 | Наименование учредителя | `itemprop="nameUchred"` | ✅ Реализовано |
| 5.2 | Юридический адрес учредителя | `itemprop="addressUchred"` | ✅ Реализовано |
| 5.3 | Контактные телефоны учредителя | `itemprop="telUchred"` | ✅ Реализовано |
| 5.4 | Адрес электронной почты учредителя | `itemprop="mailUchred"` | ✅ Реализовано |
| 5.5 | Адрес сайта учредителя | `itemprop="websiteUchred"` | ✅ Реализовано |

### Места осуществления образовательной деятельности

**Все адреса должны быть внутри контейнера `mainInfo` (для `/sveden/common`):**

| № | Проверяемая информация | Применяемые атрибуты | Статус |
|---|------------------------|---------------------|--------|
| 12 | Места при сетевой форме | `itemprop="addressPlaceSet"` | ✅ Реализовано |
| 13 | Места проведения практики | `itemprop="addressPlacePrac"` | ✅ Реализовано |
| 14 | Места практической подготовки | `itemprop="addressPlacePodg"` | ✅ Реализовано |
| 15 | Места проведения ГИА | `itemprop="addressPlaceGia"` | ✅ Реализовано |
| 16 | Места по дополнительным программам | `itemprop="addressPlaceDop"` | ✅ Реализовано |
| 17 | Места по программам проф. обучения | `itemprop="addressPlaceOpp"` | ✅ Реализовано |

**Примечание:** Основной адрес использует `itemprop="address"` (уже реализовано).

---

## РАЗДЕЛ 2: Структура и органы управления (`/sveden/struct` → endpoint: `structure`)

### 2.1. Структурные подразделения

**Контейнер:**
```html
<div class="form-data-section" itemprop="organizationStructure" itemscope itemtype="https://schema.org/Organization" role="list">
```

**Требования:**
- ✅ `itemprop="organizationStructure"` - обязателен
- ✅ `itemscope` - обязателен
- ✅ `itemtype="https://schema.org/Organization"` - обязателен
- ✅ `role="list"` - обязателен

**Элементы списка:**
```html
<div class="form-data-item" role="listitem" itemprop="organizationUnit">
    <!-- Название и описание подразделения -->
</div>
```

**Требования к элементам:**
- ✅ `role="listitem"` - обязателен
- ✅ `itemprop="organizationUnit"` - обязателен

**Статус:** ✅ Реализовано

### 2.2. Органы управления

**Контейнер:**
```html
<div class="form-data-section" itemprop="managementBodies" itemscope itemtype="https://schema.org/ItemList" role="list">
```

**Требования:**
- ✅ `itemprop="managementBodies"` - обязателен
- ✅ `itemscope` - обязателен
- ✅ `itemtype="https://schema.org/ItemList"` - обязателен
- ✅ `role="list"` - обязателен

**Элементы списка:**
```html
<div class="form-data-item" role="listitem" itemprop="managementBody">
    <!-- Название органа управления, состав, полномочия -->
</div>
```

**Требования к элементам:**
- ✅ `role="listitem"` - обязателен
- ✅ `itemprop="managementBody"` - обязателен

**Статус:** ✅ Реализовано

---

## РАЗДЕЛ 3: Документы (`/sveden/document` → endpoint: `documents`)

### Контейнер

```html
<div class="form-data-section" itemprop="documents" itemscope itemtype="https://schema.org/ItemList" role="list">
```

**Требования:**
- ✅ `itemprop="documents"` - обязателен
- ✅ `itemscope` - обязателен
- ✅ `itemtype="https://schema.org/ItemList"` - обязателен
- ✅ `role="list"` - обязателен

### Элементы списка (каждый документ)

```html
<div class="file-item" role="listitem" itemprop="document" itemscope itemtype="https://schema.org/MediaObject">
    <a href="..." itemprop="contentUrl">Скачать документ</a>
</div>
```

**Требования к элементам:**
- ✅ `role="listitem"` - обязателен
- ✅ `itemprop="document"` - обязателен
- ✅ `itemscope` - обязателен
- ✅ `itemtype="https://schema.org/MediaObject"` - обязателен
- ✅ `itemprop="contentUrl"` на ссылке - обязателен

**Статус:** ✅ Реализовано

---

## РАЗДЕЛ 4: Образование (`/sveden/education` → endpoint: `education`)

### Контейнер

```html
<div class="form-data-section" itemprop="educationalCredentialAwarded" itemscope itemtype="https://schema.org/EducationalProgram" role="list">
```

**Требования:**
- ✅ `itemprop="educationalCredentialAwarded"` - обязателен
- ✅ `itemscope` - обязателен
- ✅ `itemtype="https://schema.org/EducationalProgram"` - обязателен
- ✅ `role="list"` - обязателен

### Элементы списка (каждая программа/пункт)

```html
<div role="listitem" itemprop="educationItem">
    <!-- Информация об образовательной программе -->
</div>
```

**Требования к элементам:**
- ✅ `role="listitem"` - обязателен
- ✅ `itemprop="educationItem"` - обязателен

**Статус:** ✅ Реализовано

**Примечание:** Внутри `educationItem` могут быть дополнительные поля:
- Название программы
- Уровень образования
- Форма обучения
- Срок обучения
- Учебный план (ссылка на документ)
- Календарный учебный график (ссылка на документ)
- Численность обучающихся
- Языки обучения
- И т.д.

**⚠️ Требуется проверка:** Убедиться, что все поля внутри `educationItem` правильно структурированы.

---

## РАЗДЕЛ 5: Образовательные стандарты и требования (`/sveden/eduStandarts` → endpoint: `standards`)

### Контейнер

```html
<div class="form-data-section" itemprop="eduStandards" itemscope itemtype="https://schema.org/ItemList" role="list">
```

**Требования:**
- ✅ `itemprop="eduStandards"` - обязателен
- ✅ `itemscope` - обязателен
- ✅ `itemtype="https://schema.org/ItemList"` - обязателен
- ✅ `role="list"` - обязателен

### Элементы списка

**Статус:** ⚠️ Требуется проверка реализации

**Примечание:** Внутри могут быть:
- Ссылки на документы (использовать `itemprop="document"` + `itemscope itemtype="https://schema.org/MediaObject"`)
- Текстовые описания стандартов
- Таблицы с информацией о стандартах

---

## РАЗДЕЛ 6: Руководство (`/sveden/managers` → endpoint: `management`)

### Контейнер

```html
<div class="form-data-section" itemprop="management" itemscope itemtype="https://schema.org/ItemList" role="list">
```

**Требования:**
- ✅ `itemprop="management"` - обязателен
- ✅ `itemscope` - обязателен
- ✅ `itemtype="https://schema.org/ItemList"` - обязателен
- ✅ `role="list"` - обязателен

### Элементы списка (каждое руководящее лицо)

**Возможные поля внутри элемента:**

| Поле | Атрибут | Описание |
|------|---------|----------|
| Директор | `itemprop="director"` | ФИО директора |
| Телефон директора | `itemprop="directorPhone"` | Контактный телефон |
| Заместители | `itemprop="deputyManagement"` или `itemprop="deputy"` | Заместители директора |

**Статус:** ⚠️ Требуется проверка реализации

**Примечание:** Каждый элемент должен иметь `role="listitem"`.

---

## РАЗДЕЛ 7: Педагогический состав (`/sveden/employees` → endpoint: `teachers`)

### Контейнер

```html
<div class="form-data-section" itemprop="teachers" itemscope itemtype="https://schema.org/ItemList" role="list">
```

**Требования:**
- ✅ `itemprop="teachers"` - обязателен
- ✅ `itemscope` - обязателен
- ✅ `itemtype="https://schema.org/ItemList"` - обязателен
- ✅ `role="list"` - обязателен

### Элементы списка (каждый преподаватель)

```html
<div role="listitem" itemprop="teacher">
    <!-- ФИО, должность, образование, стаж, квалификация и т.д. -->
</div>
```

**Требования к элементам:**
- ✅ `role="listitem"` - обязателен
- ✅ `itemprop="teacher"` - обязателен

**Статус:** ⚠️ Требуется проверка реализации

---

## РАЗДЕЛ 8: Материально-техническое обеспечение и оснащённость образовательного процесса. Доступная среда (`/sveden/objects` → endpoint: `facilities`)

### Контейнер

```html
<div class="form-data-section" itemprop="facilities" itemscope itemtype="https://schema.org/ItemList" role="list">
```

**Требования:**
- ✅ `itemprop="facilities"` - обязателен
- ✅ `itemscope` - обязателен
- ✅ `itemtype="https://schema.org/ItemList"` - обязателен
- ✅ `role="list"` - обязателен

### Элементы списка

**Статус:** ⚠️ Требуется проверка реализации

**Примечание:** Внутри могут быть:
- Информация о зданиях и помещениях
- Информация об оборудовании
- Информация о библиотеке
- Информация о спортивных объектах
- Информация о доступной среде для инвалидов
- Ссылки на документы (использовать `itemprop="document"` + `itemscope itemtype="https://schema.org/MediaObject"`)
- Изображения (использовать `itemprop="photo"` + `itemscope itemtype="https://schema.org/ImageObject"`)

**Каждый элемент должен иметь:**
- `role="listitem"` - обязателен

---

## РАЗДЕЛ 9: Стипендии и меры поддержки обучающихся (`/sveden/grants` → endpoint: `scholarships`)

### Контейнер

```html
<div class="form-data-section" itemprop="scholarships" itemscope itemtype="https://schema.org/ItemList" role="list">
```

**Требования:**
- ✅ `itemprop="scholarships"` - обязателен
- ✅ `itemscope` - обязателен
- ✅ `itemtype="https://schema.org/ItemList"` - обязателен
- ✅ `role="list"` - обязателен

### Элементы списка (каждая мера поддержки)

```html
<div role="listitem" itemprop="supportMeasure">
    <!-- Название меры поддержки, условия получения, размер и т.д. -->
</div>
```

**Требования к элементам:**
- ✅ `role="listitem"` - обязателен
- ✅ `itemprop="supportMeasure"` - обязателен

**Статус:** ⚠️ Требуется проверка реализации

**Примечание:** Внутри `supportMeasure` могут быть:
- Название меры поддержки
- Условия получения
- Размер выплаты
- Ссылки на документы (использовать `itemprop="document"` + `itemscope itemtype="https://schema.org/MediaObject"`)

---

## РАЗДЕЛ 10: Платные образовательные услуги (`/sveden/paid_edu` → endpoint: `paid-services`)

### Контейнер

```html
<div class="form-data-section" itemprop="paidEduServices" itemscope itemtype="https://schema.org/ItemList" role="list">
```

**Требования:**
- ✅ `itemprop="paidEduServices"` - обязателен
- ✅ `itemscope` - обязателен
- ✅ `itemtype="https://schema.org/ItemList"` - обязателен
- ✅ `role="list"` - обязателен

### Элементы списка (каждая платная услуга)

```html
<div role="listitem" itemprop="service">
    <!-- Название услуги, стоимость, условия предоставления и т.д. -->
</div>
```

**Требования к элементам:**
- ✅ `role="listitem"` - обязателен
- ✅ `itemprop="service"` - обязателен

**Статус:** ⚠️ Требуется проверка реализации

**Примечание:** Внутри `service` могут быть:
- Название услуги
- Стоимость
- Условия предоставления
- Ссылки на документы (использовать `itemprop="document"` + `itemscope itemtype="https://schema.org/MediaObject"`)

---

## РАЗДЕЛ 11: Финансово-хозяйственная деятельность (`/sveden/budget` → endpoint: `finance`)

### Контейнер

```html
<div class="form-data-section" itemprop="financialActivity" itemscope itemtype="https://schema.org/ItemList" role="list">
```

**Требования:**
- ✅ `itemprop="financialActivity"` - обязателен
- ✅ `itemscope` - обязателен
- ✅ `itemtype="https://schema.org/ItemList"` - обязателен
- ✅ `role="list"` - обязателен

### Элементы списка (каждый отчёт)

```html
<div role="listitem" itemprop="report">
    <!-- Название отчёта, период, ссылка на документ -->
</div>
```

**Требования к элементам:**
- ✅ `role="listitem"` - обязателен
- ✅ `itemprop="report"` - обязателен

**Статус:** ⚠️ Требуется проверка реализации

**Примечание:** Внутри `report` обычно есть:
- Название отчёта
- Период отчётности
- Ссылка на документ (использовать `itemprop="document"` + `itemscope itemtype="https://schema.org/MediaObject"` + `itemprop="contentUrl"` на ссылке)

---

## РАЗДЕЛ 12: Вакантные места для приёма (перевода) обучающихся (`/sveden/vacant` → endpoint: `vacancies`)

### Контейнер

```html
<div class="form-data-section" itemprop="vacantPlaces" itemscope itemtype="https://schema.org/ItemList" role="list">
```

**Требования:**
- ✅ `itemprop="vacantPlaces"` - обязателен
- ✅ `itemscope` - обязателен
- ✅ `itemtype="https://schema.org/ItemList"` - обязателен
- ✅ `role="list"` - обязателен

### Элементы списка

**Статус:** ⚠️ Требуется проверка реализации

**Примечание:** Внутри могут быть:
- Информация о вакантных местах по уровням образования
- Информация о вакантных местах по формам обучения
- Информация о вакантных местах по классам/курсам
- Таблицы с данными

**Каждый элемент должен иметь:**
- `role="listitem"` - обязателен

**Дополнительные атрибуты:** Методичка не стандартизирует конкретные `itemprop` для элементов внутри, но структура должна быть логичной.

---

## РАЗДЕЛ 13: Международное сотрудничество (`/sveden/inter` → endpoint: `international`)

### Контейнер

```html
<div class="form-data-section" itemprop="internationalCooperation" itemscope itemtype="https://schema.org/ItemList" role="list">
```

**Требования:**
- ✅ `itemprop="internationalCooperation"` - обязателен
- ✅ `itemscope` - обязателен
- ✅ `itemtype="https://schema.org/ItemList"` - обязателен
- ✅ `role="list"` - обязателен

### Элементы списка (каждый партнёр/программа)

```html
<div role="listitem" itemprop="partner">
    <!-- Название партнёра, страна, описание сотрудничества и т.д. -->
</div>
```

**Требования к элементам:**
- ✅ `role="listitem"` - обязателен
- ✅ `itemprop="partner"` - обязателен

**Статус:** ⚠️ Требуется проверка реализации

**Примечание:** Внутри `partner` могут быть:
- Название партнёрской организации
- Страна
- Описание сотрудничества
- Ссылки на документы (использовать `itemprop="document"` + `itemscope itemtype="https://schema.org/MediaObject"`)
- Изображения (использовать `itemprop="photo"` + `itemscope itemtype="https://schema.org/ImageObject"`)

---

## РАЗДЕЛ 14: Организация питания в образовательной организации (`/sveden/catering` → endpoint: `food`)

### Контейнер

```html
<div class="form-data-section" itemprop="cateringOrganization" itemscope itemtype="https://schema.org/ItemList" role="list">
```

**Требования:**
- ✅ `itemprop="cateringOrganization"` - обязателен
- ✅ `itemscope` - обязателен
- ✅ `itemtype="https://schema.org/ItemList"` - обязателен
- ✅ `role="list"` - обязателен

### Элементы списка

**Статус:** ⚠️ Требуется проверка реализации

**Примечание:** Внутри могут быть:
- Информация об организации питания
- Меню (ссылки на документы)
- График питания
- Стоимость питания
- Ссылки на документы (использовать `itemprop="document"` + `itemscope itemtype="https://schema.org/MediaObject"`)
- Изображения (использовать `itemprop="photo"` + `itemscope itemtype="https://schema.org/ImageObject"`)

**Каждый элемент должен иметь:**
- `role="listitem"` - обязателен

**Дополнительные атрибуты:** Методичка не стандартизирует конкретные `itemprop` для элементов внутри, но структура должна быть логичной.

---

## Сводная таблица статуса реализации

| № | Раздел | Endpoint | Контейнер | Элементы списка | Статус |
|---|--------|----------|-----------|-----------------|--------|
| 1 | Основные сведения | `main` | ✅ | ✅ | ✅ Полностью реализовано |
| 2 | Структура и органы управления | `structure` | ✅ | ✅ | ✅ Полностью реализовано |
| 3 | Документы | `documents` | ✅ | ✅ | ✅ Полностью реализовано |
| 4 | Образование | `education` | ✅ | ✅ | ✅ Реализовано (требуется проверка деталей) |
| 5 | Образовательные стандарты | `standards` | ⚠️ | ⚠️ | ⚠️ Требуется проверка |
| 6 | Руководство | `management` | ⚠️ | ⚠️ | ⚠️ Требуется проверка |
| 7 | Педагогический состав | `teachers` | ⚠️ | ⚠️ | ⚠️ Требуется проверка |
| 8 | МТО и доступная среда | `facilities` | ⚠️ | ⚠️ | ⚠️ Требуется проверка |
| 9 | Стипендии и меры поддержки | `scholarships` | ⚠️ | ⚠️ | ⚠️ Требуется проверка |
| 10 | Платные образовательные услуги | `paid-services` | ⚠️ | ⚠️ | ⚠️ Требуется проверка |
| 11 | Финансово-хозяйственная деятельность | `finance` | ⚠️ | ⚠️ | ⚠️ Требуется проверка |
| 12 | Вакантные места | `vacancies` | ⚠️ | ⚠️ | ⚠️ Требуется проверка |
| 13 | Международное сотрудничество | `international` | ⚠️ | ⚠️ | ⚠️ Требуется проверка |
| 14 | Организация питания | `food` | ⚠️ | ⚠️ | ⚠️ Требуется проверка |

---

## Общие требования к микроразметке

### 1. Структура вложенности

Все разделы должны быть вложены в главный контейнер:
```html
<div itemscope itemtype="https://schema.org/EducationalOrganization">
    <!-- Все подразделы здесь -->
</div>
```

### 2. Обязательные атрибуты для контейнеров разделов

Каждый раздел должен иметь:
- `itemprop="<название_раздела>"` - связь с родительским контейнером
- `itemscope` - начало нового элемента микроразметки
- `itemtype="<тип>"` - тип данных (обычно `https://schema.org/ItemList` или `https://schema.org/EducationalOrganization`)
- `role="list"` - для списковых разделов

### 3. Обязательные атрибуты для элементов списков

Каждый элемент списка должен иметь:
- `role="listitem"` - семантическая роль элемента списка
- `itemprop="<название_элемента>"` - связь с родительским контейнером раздела

### 4. Документы и файлы

Все документы должны иметь:
```html
<div role="listitem" itemprop="document" itemscope itemtype="https://schema.org/MediaObject">
    <a href="..." itemprop="contentUrl">Скачать</a>
</div>
```

### 5. Изображения

Все изображения должны иметь:
```html
<div role="listitem" itemprop="photo" itemscope itemtype="https://schema.org/ImageObject">
    <img src="..." itemprop="contentUrl" alt="...">
</div>
```

---

## Рекомендации по исправлениям

### Приоритет 1: Критично (разделы без микроразметки)

1. **Раздел 5: Образовательные стандарты**
   - Добавить контейнер с `itemprop="eduStandards"`, `itemscope`, `itemtype="https://schema.org/ItemList"`, `role="list"`
   - Добавить `role="listitem"` для элементов списка

2. **Раздел 6: Руководство**
   - Добавить контейнер с `itemprop="management"`, `itemscope`, `itemtype="https://schema.org/ItemList"`, `role="list"`
   - Добавить элементы с `itemprop="director"`, `itemprop="directorPhone"`, `itemprop="deputyManagement"` или `itemprop="deputy"`
   - Добавить `role="listitem"` для каждого элемента

3. **Раздел 7: Педагогический состав**
   - Добавить контейнер с `itemprop="teachers"`, `itemscope`, `itemtype="https://schema.org/ItemList"`, `role="list"`
   - Добавить элементы с `itemprop="teacher"` и `role="listitem"`

4. **Разделы 8-14: Остальные разделы**
   - Добавить контейнеры с соответствующими `itemprop` атрибутами
   - Добавить `itemscope`, `itemtype="https://schema.org/ItemList"`, `role="list"`
   - Добавить `role="listitem"` для элементов списков

### Приоритет 2: Важно (улучшение существующей реализации)

1. **Раздел 1: Основные сведения**
   - ✅ Добавить `itemprop="copy"` для версии сайта для слабовидящих (если есть)

2. **Раздел 4: Образование**
   - Проверить структуру внутри `educationItem`
   - Убедиться, что все поля правильно размечены

### Приоритет 3: Желательно (оптимизация)

1. Проверить все разделы на наличие лишних/отсутствующих атрибутов
2. Убедиться, что структура вложенности соответствует методичке
3. Проверить, что все документы и изображения имеют правильную микроразметку

---

## Алгоритм проверки VIKON

Согласно методичке, алгоритм VIKON работает следующим образом:

1. **Получение страницы** подраздела с сайта
2. **Выбор всех тегов** с микроразметкой в единый список
3. **Формирование облегченного дерева** с микроразметкой:
   - Поиск родительских тегов для каждого помеченного тега
   - Формирование списка только помеченных тегов со ссылкой на родительский тег
4. **Подсчет и группировка** тегов по дереву
5. **Дополнительная проверка** и запись результатов

**Важно:** Алгоритм ищет теги с микроразметкой и строит дерево вложенности. Поэтому критично:
- Правильная вложенность тегов
- Наличие `itemscope` и `itemtype` на контейнерах
- Наличие `itemprop` на элементах

---

## Проверка текущей реализации в templates/info/section.html

### Реализованные разделы

#### ✅ Раздел 1: Основные сведения (`endpoint: main`)
- **Контейнер:** ✅ `itemprop="mainInfo"`, `itemscope`, `itemtype="https://schema.org/EducationalOrganization"`
- **Поля:** ✅ Все основные поля реализованы (fullName, shortName, regDate, address, telephone, email)
- **Учредители:** ✅ Все 5 дочерних тегов реализованы (nameUchred, addressUchred, telUchred, mailUchred, websiteUchred)
- **Режим работы:** ✅ `itemprop="workTime"` реализован
- **Адреса:** ✅ Все варианты адресов реализованы (addressPlaceSet, addressPlaceDop, addressPlaceOpp, addressPlacePrac, addressPlacePodg, addressPlaceGia)
- **Лицензия:** ✅ `itemprop="licenseDocLink"` реализован
- **Аккредитация:** ✅ `itemprop="accreditationDocLink"` реализован

#### ✅ Раздел 2: Структура и органы управления (`endpoint: structure`)
- **Структурные подразделения:** ✅ `itemprop="organizationStructure"`, `itemscope`, `itemtype="https://schema.org/Organization"`, `role="list"`
- **Элементы:** ✅ `itemprop="organizationUnit"`, `role="listitem"` реализованы
- **Органы управления:** ✅ `itemprop="managementBodies"`, `itemscope`, `itemtype="https://schema.org/ItemList"`, `role="list"`
- **Элементы:** ✅ `itemprop="managementBody"`, `role="listitem"` реализованы

#### ✅ Раздел 3: Документы (`endpoint: documents`)
- **Контейнер:** ✅ `itemprop="documents"`, `itemscope`, `itemtype="https://schema.org/ItemList"`, `role="list"`
- **Элементы:** ✅ `itemprop="document"`, `itemscope`, `itemtype="https://schema.org/MediaObject"`, `role="listitem"`, `itemprop="contentUrl"` реализованы

#### ✅ Раздел 4: Образование (`endpoint: education`)
- **Контейнер:** ✅ `itemprop="educationalCredentialAwarded"`, `itemscope`, `itemtype="https://schema.org/EducationalProgram"`, `role="list"`
- **Элементы:** ✅ `itemprop="educationItem"`, `role="listitem"` реализованы

### Разделы без специальной обработки (используют общий шаблон)

Следующие разделы не имеют специальной обработки в шаблоне и используют общий механизм отображения через `form_data` и `content_blocks`:

#### ⚠️ Раздел 5: Образовательные стандарты (`endpoint: standards`)
- **Статус:** Не реализован специальный контейнер с микроразметкой
- **Требуется:** Добавить контейнер с `itemprop="eduStandards"`, `itemscope`, `itemtype="https://schema.org/ItemList"`, `role="list"`

#### ⚠️ Раздел 6: Руководство (`endpoint: management`)
- **Статус:** Не реализован специальный контейнер с микроразметкой
- **Требуется:** Добавить контейнер с `itemprop="management"`, `itemscope`, `itemtype="https://schema.org/ItemList"`, `role="list"`
- **Требуется:** Добавить элементы с `itemprop="director"`, `itemprop="directorPhone"`, `itemprop="deputyManagement"` или `itemprop="deputy"`

#### ⚠️ Раздел 7: Педагогический состав (`endpoint: teachers`)
- **Статус:** Не реализован специальный контейнер с микроразметкой
- **Требуется:** Добавить контейнер с `itemprop="teachers"`, `itemscope`, `itemtype="https://schema.org/ItemList"`, `role="list"`
- **Требуется:** Добавить элементы с `itemprop="teacher"`, `role="listitem"`

#### ⚠️ Раздел 8: МТО и доступная среда (`endpoint: facilities`)
- **Статус:** Не реализован специальный контейнер с микроразметкой
- **Требуется:** Добавить контейнер с `itemprop="facilities"`, `itemscope`, `itemtype="https://schema.org/ItemList"`, `role="list"`

#### ⚠️ Раздел 9: Стипендии и меры поддержки (`endpoint: scholarships`)
- **Статус:** Не реализован специальный контейнер с микроразметкой
- **Требуется:** Добавить контейнер с `itemprop="scholarships"`, `itemscope`, `itemtype="https://schema.org/ItemList"`, `role="list"`
- **Требуется:** Добавить элементы с `itemprop="supportMeasure"`, `role="listitem"`

#### ⚠️ Раздел 10: Платные образовательные услуги (`endpoint: paid-services`)
- **Статус:** Не реализован специальный контейнер с микроразметкой
- **Требуется:** Добавить контейнер с `itemprop="paidEduServices"`, `itemscope`, `itemtype="https://schema.org/ItemList"`, `role="list"`
- **Требуется:** Добавить элементы с `itemprop="service"`, `role="listitem"`

#### ⚠️ Раздел 11: Финансово-хозяйственная деятельность (`endpoint: finance`)
- **Статус:** Не реализован специальный контейнер с микроразметкой
- **Требуется:** Добавить контейнер с `itemprop="financialActivity"`, `itemscope`, `itemtype="https://schema.org/ItemList"`, `role="list"`
- **Требуется:** Добавить элементы с `itemprop="report"`, `role="listitem"`

#### ⚠️ Раздел 12: Вакантные места (`endpoint: vacancies`)
- **Статус:** Не реализован специальный контейнер с микроразметкой
- **Требуется:** Добавить контейнер с `itemprop="vacantPlaces"`, `itemscope`, `itemtype="https://schema.org/ItemList"`, `role="list"`

#### ⚠️ Раздел 13: Международное сотрудничество (`endpoint: international`)
- **Статус:** Не реализован специальный контейнер с микроразметкой
- **Требуется:** Добавить контейнер с `itemprop="internationalCooperation"`, `itemscope`, `itemtype="https://schema.org/ItemList"`, `role="list"`
- **Требуется:** Добавить элементы с `itemprop="partner"`, `role="listitem"`

#### ⚠️ Раздел 14: Организация питания (`endpoint: food`)
- **Статус:** Не реализован специальный контейнер с микроразметкой
- **Требуется:** Добавить контейнер с `itemprop="cateringOrganization"`, `itemscope`, `itemtype="https://schema.org/ItemList"`, `role="list"`

---

## Детальный план исправлений

### Этап 1: Критичные разделы (5-7)

#### Задача 1.1: Раздел 5 - Образовательные стандарты
**Файл:** `templates/info/section.html`  
**Действие:** Добавить проверку `section.endpoint == 'standards'` и обернуть содержимое в контейнер:
```html
{% if section.endpoint == 'standards' %}
<div class="form-data-section" itemprop="eduStandards" itemscope itemtype="https://schema.org/ItemList" role="list">
    <!-- Существующее содержимое -->
</div>
{% endif %}
```

#### Задача 1.2: Раздел 6 - Руководство
**Файл:** `templates/info/section.html`  
**Действие:** Добавить проверку `section.endpoint == 'management'` и обернуть содержимое в контейнер:
```html
{% if section.endpoint == 'management' %}
<div class="form-data-section" itemprop="management" itemscope itemtype="https://schema.org/ItemList" role="list">
    <!-- Существующее содержимое с элементами role="listitem" -->
</div>
{% endif %}
```

#### Задача 1.3: Раздел 7 - Педагогический состав
**Файл:** `templates/info/section.html`  
**Действие:** Добавить проверку `section.endpoint == 'teachers'` и обернуть содержимое в контейнер:
```html
{% if section.endpoint == 'teachers' %}
<div class="form-data-section" itemprop="teachers" itemscope itemtype="https://schema.org/ItemList" role="list">
    <!-- Существующее содержимое с элементами role="listitem" itemprop="teacher" -->
</div>
{% endif %}
```

### Этап 2: Остальные разделы (8-14)

Аналогично добавить контейнеры для разделов 8-14 с соответствующими `itemprop` атрибутами.

---

## Следующие шаги

1. ✅ Создать отчет по тегам - **ВЫПОЛНЕНО**
2. ✅ Проверить текущую реализацию в `templates/info/section.html` - **ВЫПОЛНЕНО**
3. ⏳ Добавить недостающие теги для разделов 5-14
4. ⏳ Проверить правильность вложенности всех тегов
5. ⏳ Протестировать на сайте VIKON

---

---

## Дополнительная информация

### Content Blocks (блоки контента)

В шаблоне `section.html` есть общий механизм для отображения контента через `content_blocks`:

```html
<div class="content-blocks" role="list" itemprop="contentBlocks">
    {% for block in section.get_content_blocks() %}
        <div class="content-block" role="listitem" itemprop="contentBlock">
            <!-- Содержимое блока -->
        </div>
    {% endfor %}
</div>
```

**Микроразметка в content_blocks:**
- ✅ Контейнер: `itemprop="contentBlocks"`, `role="list"`
- ✅ Элементы: `itemprop="contentBlock"`, `role="listitem"`
- ✅ Документы внутри блоков: `itemprop="documents"`, `role="list"`, `itemprop="document"`, `itemscope itemtype="https://schema.org/MediaObject"`, `itemprop="contentUrl"`
- ✅ Фото внутри блоков: `itemprop="photos"`, `role="list"`, `itemprop="photo"`, `itemscope itemtype="https://schema.org/ImageObject"`, `itemprop="contentUrl"`

**Важно:** Content blocks используются для всех разделов, которые не имеют специальной обработки. Это означает, что разделы 5-14 могут использовать content_blocks, но им также нужны специальные контейнеры с правильными `itemprop` атрибутами согласно методичке.

### Рекомендация по реализации

Для разделов 5-14 рекомендуется:

1. **Добавить проверку endpoint** в начале каждого раздела
2. **Обернуть существующее содержимое** (form_data + content_blocks) в контейнер с правильными атрибутами
3. **Убедиться**, что элементы списков имеют `role="listitem"` и соответствующие `itemprop`

**Пример структуры для раздела 5 (Образовательные стандарты):**

```html
{% if section.endpoint == 'standards' %}
<div class="form-data-section" itemprop="eduStandards" itemscope itemtype="https://schema.org/ItemList" role="list">
    <h2>Образовательные стандарты и требования</h2>
    
    <!-- Существующие поля form_data -->
    {% if text_data.form_data %}
        <div class="form-data-grid">
            <!-- Поля формы -->
        </div>
    {% endif %}
    
    <!-- Content blocks (уже имеют микроразметку) -->
    <div class="content-blocks" role="list" itemprop="contentBlocks">
        <!-- Блоки контента -->
    </div>
</div>
{% endif %}
```

---

## Итоговая сводка

### ✅ Полностью реализовано (4 раздела)
- Раздел 1: Основные сведения
- Раздел 2: Структура и органы управления
- Раздел 3: Документы
- Раздел 4: Образование

### ⚠️ Требуется доработка (10 разделов)
- Раздел 5: Образовательные стандарты
- Раздел 6: Руководство
- Раздел 7: Педагогический состав
- Раздел 8: МТО и доступная среда
- Раздел 9: Стипендии и меры поддержки
- Раздел 10: Платные образовательные услуги
- Раздел 11: Финансово-хозяйственная деятельность
- Раздел 12: Вакантные места
- Раздел 13: Международное сотрудничество
- Раздел 14: Организация питания

### 📊 Статистика
- **Реализовано:** 4 из 14 разделов (28.6%)
- **Требуется доработка:** 10 из 14 разделов (71.4%)

---

**Дата создания отчета:** 2024  
**Статус:** Отчет завершен. Готов к использованию для исправления микроразметки.  
**Файл отчета:** `docs/MICRODATA_COMPLETE_REPORT.md`

