# Исправление ошибки удаления файлов

## 🐛 Проблема
При попытке удаления файлов возникала ошибка "Файл не найден", даже если файл существовал.

## 🔍 Причина
1. Файлы могли находиться в старой структуре папок
2. Файлы могли не быть в базе данных (не мигрированы)
3. Недостаточная обработка различных сценариев поиска файлов

## ✅ Решение

### 1. Улучшенный метод удаления файлов
**Файл**: `file_manager.py` - метод `delete_file()`

**Улучшения**:
- Добавлено подробное логирование для отладки
- Поиск файла в базе данных
- Поиск файла в новой структуре папок
- Поиск файла в старой структуре папок
- Рекурсивный поиск файла во всех подпапках
- Улучшенная обработка ошибок

### 2. Улучшенная обработка ошибок
**Файл**: `info/routes.py` - маршрут `/delete_file`

**Улучшения**:
- Если файл не найден, возвращается успешный результат с сообщением "возможно, уже удален"
- Это предотвращает показ ошибки пользователю, если файл уже был удален

## 🎯 Результат

Теперь система удаления файлов:
- ✅ **Находит файлы** в любой структуре папок
- ✅ **Не показывает ошибки** для уже удаленных файлов
- ✅ **Подробно логирует** процесс удаления для отладки
- ✅ **Работает с файлами** как из новой, так и из старой системы

## 🔧 Как это работает

1. **Поиск в БД**: Сначала ищет файл в базе данных
2. **Поиск в новой структуре**: `static/uploads/section_name/filename`
3. **Поиск в старой структуре**: `static/uploads/pages/section_name/filename`
4. **Рекурсивный поиск**: Ищет файл во всех подпапках
5. **Удаление**: Удаляет файл и запись из БД (если есть)

## 📝 Логирование

Теперь в консоли будут видны подробные сообщения:
```
DEBUG: Удаление файла filename.pdf из раздела main, поле: license_document
DEBUG: Файл не найден в БД, ищем на диске...
DEBUG: Проверяем путь: static/uploads/main/filename.pdf
DEBUG: Проверяем старый путь: static/uploads/pages/main/filename.pdf
Файл найден и удален: static/uploads/somewhere/filename.pdf
```

Это поможет понять, где именно находился файл и как происходило его удаление.


