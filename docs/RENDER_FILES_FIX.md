# Решение проблемы с файлами на Render.com

## 🔴 Проблема

Документы, загруженные локально, не отображаются на Render.com, потому что:
1. Папка `static/uploads/` в `.gitignore` - файлы не попадают в репозиторий
2. На Render.com файловая система **эфемерная** - файлы пропадают при перезапуске
3. Файлы нужно загружать **напрямую на сервер** через веб-интерфейс

## ✅ Решение 1: Загрузка файлов через веб-интерфейс на Render

### Шаги:

1. **Откройте сайт на Render.com:**
   ```
   https://site-junona.onrender.com
   ```

2. **Войдите как администратор**

3. **Загрузите файлы через мастер заполнения:**
   - Перейдите в нужный раздел (например, "Основные сведения")
   - Нажмите "⚡ Мастер" или "✏️ Редактировать"
   - Загрузите файлы через форму
   - Файлы сохранятся на сервере Render

4. **Проверьте загрузку:**
   - Файлы должны появиться в разделе
   - Ссылки должны работать

## ✅ Решение 2: Использование облачного хранилища (Рекомендуется для production)

Для production рекомендуется использовать внешнее хранилище (S3, Cloudinary и т.д.), чтобы файлы не пропадали.

### Вариант A: AWS S3

1. **Создайте аккаунт AWS S3**
2. **Создайте bucket**
3. **Установите boto3:**
   ```bash
   pip install boto3
   ```

4. **Обновите `requirements.txt`:**
   ```
   boto3>=1.28.0
   ```

5. **Добавьте переменные окружения в Render:**
   - `AWS_ACCESS_KEY_ID`
   - `AWS_SECRET_ACCESS_KEY`
   - `AWS_S3_BUCKET_NAME`
   - `AWS_REGION`

6. **Обновите `file_manager.py`** для использования S3

### Вариант B: Cloudinary (Проще в настройке)

1. **Создайте аккаунт на [cloudinary.com](https://cloudinary.com)**
2. **Установите библиотеку:**
   ```bash
   pip install cloudinary
   ```

3. **Обновите `requirements.txt`:**
   ```
   cloudinary>=1.36.0
   ```

4. **Добавьте переменные окружения в Render:**
   - `CLOUDINARY_URL` (полный URL из панели Cloudinary)

5. **Обновите `file_manager.py`** для использования Cloudinary

## ✅ Решение 3: Использование постоянного хранилища Render (если доступно)

На платном плане Render можно использовать постоянное хранилище, но на бесплатном плане это недоступно.

## ✅ Решение 4: Загрузка файлов через Render Shell

Если нужно загрузить файлы вручную:

1. **Откройте Render Shell:**
   - Render Dashboard → ваш Web Service → "Shell"

2. **Создайте необходимые папки:**
   ```bash
   mkdir -p static/uploads/info/2025/main
   mkdir -p static/uploads/info/2025/education
   # и т.д. для других разделов
   ```

3. **Загрузите файлы через SFTP или другой способ** (если доступен)

## ⚠️ Важно: Ограничения бесплатного плана Render

- **Файловая система эфемерная** - файлы пропадают при перезапуске
- **Нет постоянного хранилища** на бесплатном плане
- **Рекомендуется** использовать облачное хранилище для production

## 🔧 Временное решение: Загрузка через веб-интерфейс

Пока не настроено облачное хранилище:

1. **Загрузите все файлы через веб-интерфейс на Render**
2. **Файлы будут работать до перезапуска сервиса**
3. **После перезапуска** нужно будет загрузить файлы снова

## 📝 Рекомендации

### Для разработки:
- Используйте локальную загрузку
- Тестируйте функциональность локально

### Для production:
- **Используйте облачное хранилище** (S3, Cloudinary)
- Или используйте платный план Render с постоянным хранилищем
- Или загружайте файлы через веб-интерфейс после каждого деплоя

## 🔍 Проверка

После загрузки файлов на Render:

1. **Проверьте наличие файлов:**
   - Откройте раздел с файлами
   - Убедитесь, что ссылки работают

2. **Проверьте базу данных:**
   - Файлы должны быть в таблице `info_file`
   - Пути должны быть правильными

3. **Проверьте файловую систему (через Shell):**
   ```bash
   ls -la static/uploads/info/
   ```

---

**Примечание:** На бесплатном плане Render файлы будут пропадать при перезапуске. Для постоянного хранения используйте облачное хранилище.

