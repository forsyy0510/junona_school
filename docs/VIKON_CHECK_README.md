# Скрипт проверки микроразметки в формате VIKON

## Описание

Скрипт `check_microdata_vikon.py` полностью повторяет функциональность проверки VIKON с сайта [https://db-nica.ru/ekspress-proverka](https://db-nica.ru/ekspress-proverka).

Проверяет соответствие сайта образовательной организации "Методическим рекомендациям представления информации об образовательной организации в открытых источниках с учетом соблюдения требований законодательства в сфере образования 2024 года" (МР-2024).

## Возможности

- ✅ Проверка наличия всех обязательных разделов
- ✅ Проверка правильности структуры микроразметки (itemprop, itemscope, itemtype)
- ✅ Проверка наличия обязательных атрибутов в каждом разделе
- ✅ Проверка правильности вложенности атрибутов
- ✅ Проверка структуры документов (MediaObject)
- ✅ Проверка табличной структуры данных
- ✅ Проверка главного контейнера EducationalOrganization
- ✅ Формирование отчета в формате, аналогичном VIKON

## Установка зависимостей

```bash
pip install beautifulsoup4
```

## Использование

### Базовое использование

Проверить все разделы:

```bash
python check_microdata_vikon.py
```

### Проверка конкретного раздела

```bash
python check_microdata_vikon.py --endpoint main
```

### Сохранение отчета в файл

Текстовый формат:

```bash
python check_microdata_vikon.py --output report.txt
```

JSON формат:

```bash
python check_microdata_vikon.py --output report.json --format json
```

Оба формата:

```bash
python check_microdata_vikon.py --output report.txt --format both
```

## Проверяемые разделы

Скрипт проверяет следующие обязательные разделы согласно МР-2024:

1. **Основные сведения** (`main`)
   - Главный контейнер `mainInfo`
   - Обязательные атрибуты: `fullName`, `shortName`, `regDate`, `address`, `telephone`, `email`, `workTime`
   - Вложенная структура учредителя: `uchredLaw` → `nameUchred`, `addressUchred`, `telUchred`, `mailUchred`, `websiteUchred`

2. **Структура и органы управления** (`structure`)
   - Подразделы: `structOrgUprav`, `filInfo`, `repInfo`, `managementBodies`
   - Проверка структуры каждого подраздела

3. **Документы** (`documents`)
   - Главный контейнер `documents`
   - Структура документов: `itemscope itemtype="https://schema.org/MediaObject"`
   - Обязательный атрибут `contentUrl` в документах

4. **Образование** (`education`)
   - Главный контейнер `eduAccred`
   - Обязательные атрибуты: `eduOp`

5. **Образовательные стандарты** (`standards`)
   - Главный контейнер `eduStandards`

6. **Руководство** (`management`)
   - Главный контейнер `management`
   - Вложенные структуры: `rucovodstvo`, `rucovodstvoZam`
   - Обязательные атрибуты в `rucovodstvo`: `fio`, `post`, `telephone`, `email`

7. **Педагогический состав** (`teachers`)
   - Главный контейнер `teachingStaff`
   - Структура учителя: `itemscope itemtype="https://schema.org/Person"`

8. **Материально-техническое обеспечение** (`facilities`)
   - Главный контейнер `facilities`

9. **Стипендии и меры поддержки** (`scholarships`)
   - Главный контейнер `scholarships`

10. **Платные образовательные услуги** (`paid-services`)
    - Главный контейнер `paidEduServices`

11. **Финансово-хозяйственная деятельность** (`finance`)
    - Главный контейнер `financialActivity`

12. **Вакантные места** (`vacancies`)
    - Главный контейнер `vacantPlaces`

13. **Международное сотрудничество** (`international`)
    - Главный контейнер `internationalCooperation`

14. **Организация питания** (`food`)
    - Главный контейнер `cateringOrganization`

## Формат отчета

### Текстовый формат

```
================================================================================
ПРОВЕРКА СООТВЕТСТВИЯ МЕТОДИЧЕСКИМ РЕКОМЕНДАЦИЯМ МР-2024
Аналог проверки VIKON (https://db-nica.ru/ekspress-proverka)
================================================================================

📊 ИТОГОВАЯ СТАТИСТИКА:
   Всего разделов проверено: 14
   ✅ Разделов без проблем: 10
   ❌ Разделов с ошибками: 2
   ⚠️  Разделов с предупреждениями: 2
   Всего ошибок: 5
   Всего предупреждений: 3

================================================================================
ДЕТАЛЬНЫЙ ОТЧЕТ ПО РАЗДЕЛАМ:
================================================================================

✅ Основные сведения (main)
   URL: /sveden/common
   📊 Статистика:
      - Атрибутов itemprop: 15
      - Элементов с itemscope: 3
      - Таблиц: 1
```

### JSON формат

```json
{
  "summary": {
    "total_sections": 14,
    "sections_ok": 10,
    "sections_with_errors": 2,
    "sections_with_warnings": 2,
    "total_errors": 5,
    "total_warnings": 3
  },
  "sections": [
    {
      "name": "Основные сведения",
      "endpoint": "main",
      "url": "/sveden/common",
      "status": "ok",
      "issues": {
        "errors": [],
        "warnings": []
      },
      "stats": {
        "section_exists": true,
        "main_itemprop_found": true,
        "itemscope_correct": true,
        "itemtype_correct": true,
        "required_attrs_found": 7,
        "total_itemprop": 15,
        "total_itemscope": 3,
        "total_tables": 1
      }
    }
  ]
}
```

## Типы проблем

### Ошибки (Errors) - ❌

Критические проблемы, которые должны быть исправлены:

- Отсутствие обязательного раздела
- Отсутствие главного контейнера с `itemscope` и `itemtype`
- Отсутствие обязательных атрибутов
- Неправильная структура вложенности
- Отсутствие главного контейнера EducationalOrganization

### Предупреждения (Warnings) - ⚠️

Проблемы, которые желательно исправить:

- Использование старой структуры вместо таблиц
- Отсутствие `itemprop="name"` в главном контейнере
- Неправильная структура таблиц

## Статусы разделов

- ✅ **ok** - Раздел полностью соответствует требованиям
- ❌ **error** - Раздел содержит критические ошибки
- ⚠️ **warning** - Раздел содержит предупреждения

## Сравнение с оригинальным скриптом

| Функция | `check_microdata.py` | `check_microdata_vikon.py` |
|---------|---------------------|---------------------------|
| Проверка главного контейнера | ❌ | ✅ |
| Проверка наличия разделов | ⚠️ | ✅ |
| Детальная проверка структуры | ⚠️ | ✅ |
| Проверка вложенных структур | ✅ | ✅ |
| Проверка структуры документов | ⚠️ | ✅ |
| Формат отчета VIKON | ❌ | ✅ |
| JSON отчет | ❌ | ✅ |
| Классификация проблем | ⚠️ | ✅ |

## Рекомендации

1. **Запускайте проверку регулярно** после внесения изменений в шаблоны
2. **Исправляйте ошибки в первую очередь** - они критичны для прохождения проверки VIKON
3. **Обращайте внимание на предупреждения** - они могут указывать на потенциальные проблемы
4. **Используйте JSON формат** для автоматической обработки результатов
5. **Проверяйте конкретные разделы** при разработке новых функций

## Связь с VIKON

Скрипт полностью повторяет логику проверки экспресс-проверки VIKON:

- ✅ Проверяет те же разделы
- ✅ Проверяет те же атрибуты
- ✅ Использует те же критерии оценки
- ✅ Формирует аналогичный отчет

**Важно:** Скрипт проверяет локальную версию сайта. Для проверки на сайте VIKON используйте [https://db-nica.ru/ekspress-proverka](https://db-nica.ru/ekspress-proverka).

## Поддержка

При возникновении проблем:

1. Проверьте, что все зависимости установлены
2. Убедитесь, что сервер запущен
3. Проверьте правильность URL разделов
4. Просмотрите детальный отчет для выявления проблем

