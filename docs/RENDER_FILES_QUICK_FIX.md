# Быстрое решение: Файлы не отображаются на Render.com

## 🔴 Проблема

Документы, загруженные локально, не отображаются на Render.com.

## ✅ Решение (5 минут)

### Шаг 1: Откройте сайт на Render
```
https://site-junona.onrender.com
```

### Шаг 2: Войдите как администратор

### Шаг 3: Загрузите файлы через веб-интерфейс

1. Перейдите в нужный раздел (например, "Основные сведения")
2. Нажмите **"⚡ Мастер"** или **"✏️ Редактировать"**
3. Загрузите файлы через форму загрузки
4. Сохраните изменения

### Шаг 4: Проверьте

- Файлы должны появиться в разделе
- Ссылки должны работать

## ⚠️ Важно

**На Render.com файловая система эфемерная** - файлы могут пропадать при перезапуске сервиса.

### Что это значит:
- Файлы, загруженные через веб-интерфейс, работают **до перезапуска**
- После перезапуска нужно загрузить файлы **снова**

### Рекомендации:
1. **Для тестирования:** Загружайте файлы через веб-интерфейс
2. **Для production:** Используйте облачное хранилище (S3, Cloudinary) - см. `docs/RENDER_FILES_FIX.md`

## 🔍 Если файлы все еще не работают

1. **Проверьте логи на Render:**
   - Render Dashboard → ваш Web Service → "Logs"
   - Ищите ошибки типа "File not found"

2. **Проверьте базу данных:**
   - Файлы должны быть в таблице `info_file`
   - Пути должны быть правильными

3. **Проверьте файловую систему (через Shell):**
   ```bash
   ls -la static/uploads/info/
   ```

---

**Примечание:** Это временное решение. Для постоянного хранения файлов используйте облачное хранилище (см. `docs/RENDER_FILES_FIX.md`).

