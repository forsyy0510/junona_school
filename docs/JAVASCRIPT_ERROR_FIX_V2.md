# Исправление ошибки JavaScript - версия 2

## 🐛 **Проблема**
Ошибка `toggleInputType is not defined` все еще возникала, несмотря на предыдущие исправления.

## 🔍 **Причина**
Проблема была в том, что в HTML коде использовались вызовы `wizardManager.toggleInputType()`, но `wizardManager` мог быть не инициализирован на момент вызова.

## ✅ **Решение**

### 1. **Перемещен экспорт функции в начало файла**
```javascript
// Экспортируем функцию toggleInputType для глобального доступа (до инициализации)
window.toggleInputType = (fieldName, type) => {
    if (window.wizardManager && typeof window.wizardManager.toggleInputType === 'function') {
        return window.wizardManager.toggleInputType(fieldName, type);
    } else {
        console.warn('toggleInputType: wizardManager не инициализирован');
    }
};
```

### 2. **Исправлены вызовы в HTML коде**
Заменили `wizardManager.toggleInputType()` на `toggleInputType()` в HTML:
```html
<!-- Было -->
onclick="wizardManager.toggleInputType('${field.name}', 'url')"

<!-- Стало -->
onclick="toggleInputType('${field.name}', 'url')"
```

### 3. **Добавлен fallback в app.js**
```javascript
if (!window.toggleInputType) {
    window.toggleInputType = (fieldName, type) => {
        console.warn('toggleInputType: wizardManager не инициализирован, попробуйте позже');
        // Попробуем снова через небольшую задержку
        setTimeout(() => {
            if (window.wizardManager && typeof window.wizardManager.toggleInputType === 'function') {
                return window.wizardManager.toggleInputType(fieldName, type);
            }
        }, 100);
    };
}
```

### 4. **Обновлена версия файла**
Изменена версия `wizard.js` с `v=4` на `v=6` для обхода кэширования.

## 🎯 **Результат**

- ✅ **Функция экспортируется до инициализации** - доступна сразу после загрузки скрипта
- ✅ **Исправлены вызовы в HTML** - используются глобальные функции вместо методов объекта
- ✅ **Добавлен fallback** - система работает даже если что-то пошло не так
- ✅ **Обновлен кэш** - браузер загрузит новую версию файла

## 🔧 **Как это работает**

1. **Загрузка скрипта**: `wizard.js` загружается и сразу экспортирует `toggleInputType`
2. **HTML вызовы**: Кнопки в HTML вызывают глобальную функцию `toggleInputType()`
3. **Делегирование**: Функция проверяет, инициализирован ли `wizardManager`, и делегирует вызов
4. **Fallback**: Если что-то пошло не так, `app.js` предоставляет резервную функцию

## 📝 **Файлы изменены**

- `static/wizard.js` - перемещен экспорт функции, исправлены HTML вызовы
- `static/app.js` - добавлен fallback
- `templates/base.html` - обновлена версия файла

Теперь система должна работать без ошибок! 🎉
