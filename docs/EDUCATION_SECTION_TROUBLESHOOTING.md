# Устранение проблем с разделом "Образование"

## 🔍 Диагностика

Если страница "Образование" не отображается, выполните следующие шаги:

### 1. Проверьте наличие раздела в БД

Запустите скрипт проверки:

```bash
python check_education_section.py
```

Или используйте улучшенный скрипт:

```bash
python check_education_section_fix.py
```

### 2. Проверьте URL

Раздел должен быть доступен по URL:
- `/sveden/education` (основной)
- `/info/education` (старый, редирект)

### 3. Проверьте маппинг endpoint'ов

В файле `info/routes.py` должен быть маппинг:
```python
ENDPOINT_MAPPING = {
    ...
    'education': 'education',  # Образование
    ...
}
```

### 4. Проверьте автоматическое создание

В файле `app.py` должно быть автоматическое создание раздела при запуске.

## ✅ Решения

### Решение 1: Создать раздел вручную

Запустите скрипт:

```bash
python check_education_section.py
```

### Решение 2: Создать через мастер заполнения

1. Войдите как администратор
2. Перейдите в "Сведения об образовательной организации"
3. Нажмите "⚡ Мастер"
4. Выберите "Образование"
5. Заполните и сохраните

### Решение 3: Проверить логи

Проверьте логи приложения на наличие ошибок:

```bash
# В логах должны быть сообщения:
# "Автоматическое создание раздела 'Образование'"
# или
# "Раздел 'Образование' успешно создан"
```

### Решение 4: Перезапустить приложение

После создания раздела перезапустите приложение:

```bash
# Локально
python app.py

# На Render.com
# Через Dashboard -> Manual Deploy
```

## 🔧 Проверка через Python shell

```python
from app import create_app, db
from info.models import InfoSection

app = create_app()
with app.app_context():
    # Проверяем раздел
    section = InfoSection.query.filter_by(endpoint='education').first()
    if section:
        print(f"✅ Раздел найден: {section.title}")
        print(f"   URL: {section.url}")
        print(f"   Endpoint: {section.endpoint}")
    else:
        print("❌ Раздел не найден")
        
        # Создаем раздел
        import json
        section = InfoSection(
            endpoint='education',
            url='/sveden/education',
            title='Образование',
            text=json.dumps({
                'form_data': {
                    'title': 'Образование',
                    'implemented_programs': '',
                    'adapted_programs': '',
                    'curriculum_noo': '',
                    'curriculum_ooo': '',
                    'curriculum_soo': '',
                    'calendar_schedule': '',
                    'student_numbers_noo': '',
                    'student_numbers_ooo': '',
                    'student_numbers_soo': '',
                    'student_numbers_document': '',
                    'education_languages': '',
                    'professional_programs': '',
                    'graduate_employment': ''
                }
            }, ensure_ascii=False),
            content_blocks=json.dumps([], ensure_ascii=False)
        )
        db.session.add(section)
        db.session.commit()
        print("✅ Раздел создан!")
```

## 📋 Чеклист

- [ ] Раздел существует в БД (`endpoint='education'`)
- [ ] URL правильный (`/sveden/education`)
- [ ] Маппинг настроен (`'education': 'education'`)
- [ ] Blueprint зарегистрирован (`/sveden` и `/info`)
- [ ] Автоматическое создание работает
- [ ] Нет ошибок в логах

## ⚠️ Частые проблемы

### Проблема 1: Раздел не создается автоматически

**Решение:** Проверьте логи при запуске. Если есть ошибки, создайте раздел вручную через скрипт.

### Проблема 2: URL не работает

**Решение:** Проверьте регистрацию blueprint'ов в `app.py`:
```python
app.register_blueprint(info_bp, url_prefix='/info')
app.register_blueprint(info_bp, url_prefix='/sveden', name='sveden_bp')
```

### Проблема 3: Раздел есть, но не отображается

**Решение:** Проверьте шаблон `templates/info/section.html` и убедитесь, что он правильно обрабатывает раздел "Образование".

## 🚀 После исправления

1. Перезапустите приложение
2. Откройте `/sveden/education` в браузере
3. Раздел должен отобразиться

---

**Если проблема сохраняется**, проверьте логи приложения и убедитесь, что все зависимости установлены правильно.

