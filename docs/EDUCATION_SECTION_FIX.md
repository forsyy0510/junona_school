# Исправление проблемы с разделом "Образование"

## Проблема

Раздел "Образование" недоступен по URL `/sveden/education`, хотя ссылка есть в меню.

## ✅ Решение

Добавлено **автоматическое создание раздела** при первом обращении к URL `/sveden/education`.

### Что сделано:

1. **Автоматическое создание в `app.py`** - раздел создается при запуске приложения
2. **Автоматическое создание в `info/routes.py`** - раздел создается при первом обращении к URL, если его нет

## 🔍 Проверка

### Вариант 1: Просто откройте страницу

Откройте в браузере:
```
https://site-junona.onrender.com/sveden/education
```

Раздел создастся автоматически при первом обращении.

### Вариант 2: Проверьте через скрипт

```bash
python check_education_section.py
```

### Вариант 3: Проверьте в базе данных

```python
from app import app, db
from info.models import InfoSection

with app.app_context():
    section = InfoSection.query.filter_by(endpoint='education').first()
    if section:
        print(f"✅ Раздел найден: {section.title}")
        print(f"   URL: {section.url}")
        print(f"   ID: {section.id}")
    else:
        print("❌ Раздел не найден")
```

## 📋 Что происходит при обращении к `/sveden/education`

1. Запрос приходит на маршрут `@info_bp.route('/<section_endpoint>')`
2. `section_endpoint = 'education'`
3. Проверяется маппинг: `ENDPOINT_MAPPING.get('education', 'education')` → `'education'`
4. Ищется раздел в БД: `InfoSection.query.filter_by(endpoint='education').first()`
5. **Если раздела нет** → создается автоматически
6. Раздел отображается

## ⚠️ Важно

- Раздел создается с пустыми полями
- После создания заполните его через мастер заполнения
- URL должен быть `/sveden/education` (не `/info/education`)

## 🔧 Если проблема сохраняется

1. **Проверьте логи приложения** - должны быть сообщения о создании раздела
2. **Проверьте базу данных** - раздел должен появиться в таблице `info_section`
3. **Очистите кэш браузера** - возможно, старая версия страницы закэширована
4. **Перезапустите приложение** - на Render.com это делается через "Manual Deploy"

## 📝 После создания раздела

1. Заполните раздел через мастер заполнения:
   - Войдите как администратор
   - Перейдите в "Сведения об образовательной организации"
   - Нажмите "⚡ Мастер"
   - Выберите "Образование"
   - Заполните все обязательные поля

2. Загрузите документы:
   - Учебные планы (НОО, ООО, СОО)
   - Календарный учебный график
   - Документ о численности обучающихся
   - И другие необходимые документы

3. Проверьте работу:
   - URL должен открываться без ошибок
   - Все поля должны отображаться
   - Документы должны скачиваться

---

**Примечание:** Теперь раздел "Образование" создается автоматически при первом обращении к URL `/sveden/education`. Просто откройте эту страницу в браузере.

